# Leave unset in dev: requests go through the Vite dev proxy to
# http://localhost:3000 (see vite.config.ts). Set only when the frontend is
# deployed separately from the backend and the backend has CORS configured
# for this origin.
VITE_API_BASE_URL=
